export type NavLink = {
  href: string;
  label: string;
};

export type SocialLink = {
  name: string;
  url: string;
  icon: string;
};

export type ContactInfo = {
  phone: string;
  email: string;
  address: string;
};
